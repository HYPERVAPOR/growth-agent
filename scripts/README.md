# 手动触发脚本使用指南

## 🚀 快速开始

### 方法1: 使用独立脚本（推荐 - 可以分步执行）

```bash
# 1. 同步内容（从X和RSS获取）
python scripts/sync_content.py

# 2. 评估内容（LLM评分筛选）
python scripts/curate_content.py

# 3. 生成博客
python scripts/generate_blog.py
```

### 方法2: 一键运行所有步骤

```bash
# 使用bash脚本
./scripts/run_all.sh

# 或使用CLI
python -m growth_agent.main run workflow-b
```

## 📋 脚本说明

### 1. sync_content.py - 内容同步

**功能**: 从X创作者和RSS源获取最新内容

**输入**:
- `data/subscriptions/x_creators.jsonl` - X创作者订阅
- `data/subscriptions/rss_feeds.jsonl` - RSS源订阅

**输出**:
- `data/inbox/items.jsonl` - 原始获取的内容

**示例输出**:
```
============================================================
手动触发内容同步
============================================================

[1/2] 从X创作者获取推文...
找到 20 个X创作者订阅
  ✓ @OpenAI: 15 条推文
  ✓ @sama: 3 条推文
  ...

[2/2] 从RSS源获取文章...
找到 18 个RSS源订阅
  ✓ OpenAI Blog: 2 篇文章
  ✓ Google DeepMind Blog: 1 篇文章
  ...

✓ 成功获取 50 条内容
  已保存到: data/inbox/items.jsonl
```

**何时使用**:
- 想先获取内容，稍后评估
- 测试新的订阅源
- 调试API连接问题

---

### 2. curate_content.py - 内容评估

**功能**: 使用LLM评估inbox中的内容并筛选高质量内容

**输入**:
- `data/inbox/items.jsonl` - 待评估内容

**输出**:
- `data/curated/{YYYY-MM-DD}_ranked.jsonl` - 精选内容

**配置**:
- `CURATION_MIN_SCORE=60` - 最低评分（默认60）
- `CURATION_TOP_K=10` - 最多保留前K个（默认10）

**示例输出**:
```
============================================================
手动触发内容评估
============================================================

读取inbox内容...
找到 50 条待评估内容

开始LLM评估...
(这可能需要几分钟，请耐心等待...)

✓ 成功评估 50 条内容

过滤和排序 (分数 >= 60, 前10名)...
✓ 筛选出 10 条高质量内容

评分统计:
  平均分: 72.3
  最高分: 92
  最低分: 45

🏆 前10名:
  1. [92分] Yann LeCun explains the key to AGI...
  2. [88分] OpenAI announces new model...
  3. [85分] DeepMind's latest research...
  ...

✓ 已保存到: data/curated/2026-02-05_ranked.jsonl
```

**何时使用**:
- 在同步内容后
- 想查看评估结果
- 调整评分标准后重新评估

---

### 3. generate_blog.py - 博客生成

**功能**: 基于精选内容生成博客文章

**输入**:
- `data/curated/{YYYY-MM-DD}_ranked.jsonl` - 精选内容

**输出**:
- `data/blogs/{ID}_{SLUG}.md` - 生成的博客（带YAML frontmatter）

**示例输出**:
```
============================================================
手动触发博客生成
============================================================

读取今天的精选内容 (2026-02-05)...
找到 10 条精选内容

开始生成博客...
(这可能需要几分钟，请耐心等待...)

✓ 博客生成成功!

文件: data/blogs/a1b2c3d4_ai-insights-daily.md
标题: AI Insights: Key Developments in February 2026
长度: 1250 字符

内容预览:
  Daily curated insights on AI and technology
  标签: AI, Technology, LLMs

✓ 已归档精选内容到: data/curated/archives/2026-02-05_ranked.jsonl
```

**何时使用**:
- 在评估内容后
- 生成每日博客
- 测试不同的博客风格

---

### 4. run_all.sh - 完整流程

**功能**: 一键运行所有三个阶段

**等同于**: 依次运行 `sync_content.py` → `curate_content.py` → `generate_blog.py`

**示例输出**:
```
==================================================
Growth Agent - 完整流程手动执行
==================================================

[1/3] 同步内容...
...

[2/3] 评估内容...
...

[3/3] 生成博客...
...

==================================================
✓ 所有步骤完成!
==================================================
```

**何时使用**:
- 想一键完成整个流程
- 定期批量执行
- 不需要分步查看中间结果

---

## 🔧 高级用法

### 查看inbox内容

```bash
# 查看前5条
head -5 data/inbox/items.jsonl | jq

# 统计数量
wc -l data/inbox/items.jsonl

# 查看来源分布
jq -r '.source' data/inbox/items.jsonl | sort | uniq -c
```

### 只同步X或RSS

修改脚本临时注释掉不需要的部分：

```python
# sync_content.py 第45-48行
# RSS ingestion (暂时注释)
# for feed in rss_feeds:
#     ...
```

### 调整评分阈值

```bash
# 临时提高评分标准到70分
CURATION_MIN_SCORE=70 python scripts/curate_content.py

# 只保留前5名
CURATION_TOP_K=5 python scripts/curate_content.py
```

### 测试单个订阅源

```python
# 创建测试脚本
from growth_agent.ingestors.x_twitter import XTwitterIngestor
from growth_agent.config import Settings

settings = Settings()
ingestor = XTwitterIngestor(settings)

# 只测试一个账号
items = ingestor.fetch_creator_tweets("1605", "sama")
print(f"Fetched {len(items)} tweets from @sama")
```

---

## 📊 工作流程对比

| 方法 | 优点 | 缺点 | 适用场景 |
|------|------|------|----------|
| **独立脚本** | 分步执行，可调试，看到中间结果 | 需要多次运行 | 开发、测试、调试 |
| **CLI命令** | 一条命令，自动清理，日志完整 | 看不到中间过程 | 生产环境、自动化 |
| **完整脚本** | 完整流程，无需干预 | 无法单独控制某一步 | 定期批量执行 |

---

## 🐛 故障排查

### 问题1: 同步失败，获取0条内容

**可能原因**:
- API密钥无效或过期
- 网络连接问题
- 订阅列表为空

**解决方法**:
```bash
# 检查API密钥
cat .env | grep API_KEY

# 检查订阅
wc -l data/subscriptions/*.jsonl

# 测试API连接
curl -X GET "https://twitter241.p.rapidapi.com/user?username=OpenAI" \
  -H "x-rapidapi-key: YOUR_KEY"
```

### 问题2: LLM评估失败

**可能原因**:
- OpenRouter API密钥问题
- 内容过长超出token限制
- 提示词文件缺失

**解决方法**:
```bash
# 检查密钥
cat .env | grep OPENROUTER

# 检查提示词文件
ls prompts/*.txt

# 查看日志
tail -50 data/logs/$(date +%Y-%m-%d).log
```

### 问题3: 博客生成失败

**可能原因**:
- 没有精选内容
- LLM返回格式错误
- 文件权限问题

**解决方法**:
```bash
# 检查精选内容
ls data/curated/*_ranked.jsonl

# 查看精选内容数量
jq '. | length' data/curated/2026-02-05_ranked.jsonl

# 检查blogs目录权限
ls -la data/blogs/
```

---

## 💡 最佳实践

1. **首次使用**: 先运行独立脚本，熟悉每个阶段
2. **定期运行**: 使用CLI或完整脚本，自动化执行
3. **调试问题**: 使用独立脚本，查看详细输出
4. **调整参数**: 通过环境变量或修改配置
5. **查看日志**: 检查 `data/logs/` 目录了解详情

---

## 📞 快速命令参考

```bash
# 完整workflow
python -m growth_agent.main run workflow-b

# 分步执行
python scripts/sync_content.py           # 同步
python scripts/curate_content.py          # 评估
python scripts/generate_blog.py           # 生成

# 一键运行
./scripts/run_all.sh

# 查看日志
tail -f data/logs/$(date +%Y-%m-%d).log

# 检查状态
echo "Inbox: $(wc -l < data/inbox/items.jsonl) 条"
echo "Curated: $(ls data/curated/*_ranked.jsonl 2>/dev/null | wc -l) 个文件"
echo "Blogs: $(ls data/blogs/*.md 2>/dev/null | wc -l) 篇"
```
